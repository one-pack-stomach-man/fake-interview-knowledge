/**
 * opacity: 0、visibility: hidden、display: none 
 * display: none;
 *      渲染树中没有这个元素 
 *  
 * visibility: hidden:
 *      元素不可见 但是仍然占据空间 不可点击
 * 
 * opacity: 0;
 *      元素不可见 占据空间 可以点击
 * 
 * none 是母元素不渲染 导致不显示 
 * hidden 继承 导致子元素不显示
 * 
 * 
 */